# بناء التطبيق عبر GitHub Actions

## تم الإعداد بنجاح! 🎉

تم إعداد GitHub Actions تلقائياً لبناء التطبيق كلما قمت برفع الكود.

## الخطوات التالية:

### 1. رفع الكود إلى GitHub
```bash
git init
git add .
git commit -m "Initial commit - WhatsApp Status Saver"
git branch -M main
git remote add origin https://github.com/yourusername/save_status.git
git push -u origin main
```

### 2. تشغيل البناء يدوياً
1. اذهب إلى صفحة المشروع على GitHub
2. اضغط على "Actions"
3. اختر "Build Android APK"
4. اضغط "Run workflow"

### 3. تحميل APK
1. انتظر حتى ينتهي البناء (5-10 دقائق)
2. اذهب إلى قسم "Artifacts"
3. حمل ملف "debug-apk"
4. أو حمل من "Releases" إذا تم إنشاؤها

## ماذا يحدث بعد رفع الكود؟

✅ **تلقائياً:**
- بناء التطبيق
- إنشاء ملف APK
- إصدار جديد (إذا كان على الفرع الرئيسي)
- رفع APK كملف قابل للتحميل

## تحميل APK على هاتفك:

1. **حمل الملف** من GitHub إلى جهاز الكمبيوتر
2. **انقل الملف** إلى هاتفك (USB, WhatsApp, Email)
3. **فعّل التثبيت من مصادر غير معروفة:**
   - الإعدادات > الأمان > تفعيل "مصادر غير معروفة"
4. **اضغط على ملف APK** واتبع التعليمات

## ملاحظات هامة:

- **البناء الأولي** يستغرق 10-15 دقيقة
- **البناءات التالية** أسرع (5-7 دقائق)
- **ملف APK** صالح لمدة 30 يوم
- **لا حاجة** لـ Android Studio

## استكشاف الأخطاء:

- **فشل البناء:** تحقق من ملفات Gradle
- **لا يوجد APK:** انتظر حتى ينتهي البناء بالكامل
- **مشاكل التثبيت:** تأكد من تفعيل مصادر غير معروفة

هل تريد مساعدة في رفع الكود إلى GitHub الآن؟
